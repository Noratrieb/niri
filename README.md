# niri spawn perf

Testing the impact of waiting for the systemd scope creation in the spawned process to fix https://github.com/niri-wm/niri/issues/3201.

To run:
```
cargo build --release
cargo run --release --bin scratchSoXjl4wQ3
```
